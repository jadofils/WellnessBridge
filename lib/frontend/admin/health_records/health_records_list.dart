import 'package:flutter/material.dart';

class HealthRecordsList extends StatelessWidget {
  const HealthRecordsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Health Records List'));
  }
} 